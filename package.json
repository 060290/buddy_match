{
  "name": "buddy-match",
  "private": true,
  "description": "BuddyMatch – safe meetups for reactive dog owners",
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "setup": "cd backend && npm install && npx prisma generate && npx prisma db push && node prisma/seed.js && cd ../frontend && npm install",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
