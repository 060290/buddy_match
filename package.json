{
  "name": "buddy-match",
  "private": true,
  "description": "BuddyMatch – safe meetups for reactive dog owners",
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "setup": "cd backend && npm install && npx prisma generate && npx prisma db push && node prisma/seed.js && cd ../frontend && npm install",
    "build": "cd frontend && rm -rf node_modules package-lock.json && npm install && npm run build",
    "deploy:build": "npm install && cd frontend && rm -rf node_modules package-lock.json .vite && npm install && npm run build && cd ../backend && npm install && npx prisma generate && npx prisma db push && node prisma/seed.js",
    "start": "cd backend && node src/index.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
